export class Field {
    constructor(options) {
         this.id = options.id;
         this.state = options.state;
         this.isLast = options.isLast;
    }

    setState() {

    }

    getFieldById(id) {
        
    }
}